version: 2.1

orbs:
  greeting: testorb/testorb@1.1.0

jobs:
  greet-job:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          command: 'echo "Local fallback: Hello World"'

workflows:
  test:
    jobs:
      - greet-job:
          override-with: greeting/greet
